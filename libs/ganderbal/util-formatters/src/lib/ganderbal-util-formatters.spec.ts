import { ganderbalUtilFormatters } from './ganderbal-util-formatters';

describe('ganderbalUtilFormatters', () => {
  it('should work', () => {
    expect(ganderbalUtilFormatters()).toEqual('ganderbal-util-formatters');
  });
});
